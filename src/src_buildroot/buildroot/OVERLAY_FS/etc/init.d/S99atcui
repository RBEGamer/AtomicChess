#!/bin/sh
# /etc/init.d/S99atc ATCProject Marcel Ochsendorf marcelochsendorf.com
#
# atc        Starts ATC pacakges.
#



umask 077

start() {
	printf "Starting ATC GUI"
	touch /usr/ATC/ATC_LOCK_GUI
    /usr/ATC/start_atc_ui.sh &
	echo "OK"
}
stop() {
	printf "Stopping ATC GUI"
	rm -f /usr/ATC/	touch /usr/ATC/ATC_LOCK_GUI
	killall atc_ui
	echo "OK"
}
restart() {
	stop
	start
}

case "$1" in
  start)
	start
	;;
  stop)
	stop
	;;
  restart|reload)
	restart
	;;
  *)
	echo "Usage: $0 {start|stop|restart}"
	exit 1
esac

exit $?


